<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <title>Document</title>
  </head>

  <body>
    <div class="container">
      <form action="/file/update/<%= id %>" method="POST" enctype="multipart/form-data">
        <div class="mb-3">
          <label for="nama" class="form-label">File</label>
          <input
            type="file"
            class="form-control"
            name="file_pdf"
            value="<%= file_pdf %>"
          />
        </div>
        <div class="mb-3">
          <label for="nama" class="form-label">Nama File</label>
          <input
            type="text"
            class="form-control"
            name="nama_file"
            value="<%= nama_file %>"
            required
          />
        </div>
        <div class="mb-3">
          <label for="nama" class="form-label">Kategori</label>
          <select class="form-select" name="id_kategori" required>
            <option value="<%= kategoriById.id_kategori %>" selected ><%= kategoriById.nama_kategori %></option>
            <% for(let i = 0; i < kategoriData.length; i++) { %>
            <option value="<%= kategoriData[i].id_kategori %>" ><%= kategoriData[i].nama_kategori %></option>
            <% } %>
          </select>
        </div>
        <div class="mb-3">
          <label for="nama" class="form-label">Deskripsi</label>
          <input
            type="text"
            class="form-control"
            name="deskripsi"
            value="<%= deskripsi %>"
            required
          />
        </div>
        <div class="mb-3">
          <label for="privasi" class="form-label">Privasi</label>
          <select class="form-select" name="privasi" required>
            <option value="<%= privasi %>" selected><%= privasi %></option>
            <option value="public">public</option>
            <option value="private">private</option>
          </select>
        </div>
        <div class="mb-3">
          <label for="izin" class="form-label">Izin</label>
          <select class="form-select" name="izin" required>
            <option value="<%= izin %>" selected><%= izin %></option>
            <option value="Salin dan rekatkan teks">Salin dan rekatkan teks</option>
            <option value="Unduh dokumen">Unduh dokumen</option>
          </select>
        </div>
        <div class="mb-3">
          <label for="hak_cipta" class="form-label">Hak Cipta</label>
          <select class="form-select" name="hak_cipta" required>
            <option value="<%= hak_cipta %>" selected><%= hak_cipta %></option>
            <option value="All Rights Reserved">All Rights Reserved</option>
            <option value="Public Domain">Public Domain</option>
          </select>
        </div>
        <div class="mb-3">
          <button type="submit" class="btn btn-sm btn-success">Simpan</button>
        </div>
      </form>
    </div>
  </body>
  <script
    src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
    crossorigin="anonymous"
  ></script>
</html>
