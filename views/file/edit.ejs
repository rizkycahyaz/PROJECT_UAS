<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Form Edit File</title>
    <!-- Bootstrap CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <style>
      /* Custom CSS styles */
      .card {
        max-width: 600px; /* Lebar maksimum card */
        margin: 0 auto; /* Posisi tengah horizontal */
        margin-top: 20px; /* Jarak atas */
        border-radius: 10px; /* Sudut melengkung */
        padding: 20px; /* Padding dalam card */
      }

      .form-control {
        height: 40px; /* Tinggi kontrol input */
      }

      .form-select {
        height: 40px; /* Tinggi kontrol select */
      }

      .btn {
        height: 40px; /* Tinggi tombol */
        padding: 8px 16px; /* Padding tombol */
      }

      /* Mengurangi margin antar elemen dalam form */
      .mb-3 {
        margin-bottom: 10px; /* Ubah sesuai kebutuhan */
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="card shadow">
        <div class="card-body">
          <h2 class="card-title mb-4">Edit File</h2>
          <form
            action="/file/update/<%= id %>"
            method="POST"
            enctype="multipart/form-data"
          >
            <!-- File Input -->
            <div class="mb-3">
              <label for="file_pdf" class="form-label">File</label>
              <input
                type="file"
                class="form-control form-control-file"
                id="file_pdf"
                name="file_pdf"
                accept=".pdf"
              />
            </div>

            <!-- Nama File -->
            <div class="mb-3">
              <label for="nama_file" class="form-label">Nama File</label>
              <input
                type="text"
                class="form-control"
                id="nama_file"
                name="nama_file"
                value="<%= nama_file %>"
                required
              />
            </div>

            <!-- Kategori -->
            <div class="mb-3">
              <label for="id_kategori" class="form-label">Kategori</label>
              <select
                class="form-select"
                id="id_kategori"
                name="id_kategori"
                required
              >
                <option value="<%= kategoriById.id_kategori %>" selected>
                  <%= kategoriById.nama_kategori %>
                </option>
                <% for(let i = 0; i < kategoriData.length; i++) { %>
                <option value="<%= kategoriData[i].id_kategori %>">
                  <%= kategoriData[i].nama_kategori %>
                </option>
                <% } %>
              </select>
            </div>

            <!-- Deskripsi -->
            <div class="mb-3">
              <label for="deskripsi" class="form-label">Deskripsi</label>
              <input
                type="text"
                class="form-control"
                id="deskripsi"
                name="deskripsi"
                value="<%= deskripsi %>"
                required
              />
            </div>

            <!-- Privasi -->
            <div class="mb-3">
              <label for="privasi" class="form-label">Privasi</label>
              <select class="form-select" id="privasi" name="privasi" required>
                <option value="<%= privasi %>" selected><%= privasi %></option>
                <option value="public">Public</option>
                <option value="private">Private</option>
              </select>
            </div>

            <!-- Izin -->
            <div class="mb-3">
              <label for="izin" class="form-label">Izin</label>
              <select class="form-select" id="izin" name="izin" required>
                <option value="<%= izin %>" selected><%= izin %></option>
                <option value="Salin dan rekatkan teks">
                  Salin dan rekatkan teks
                </option>
                <option value="Unduh dokumen">Unduh dokumen</option>
              </select>
            </div>

            <!-- Hak Cipta -->
            <div class="mb-3">
              <label for="hak_cipta" class="form-label">Hak Cipta</label>
              <select
                class="form-select"
                id="hak_cipta"
                name="hak_cipta"
                required
              >
                <option value="<%= hak_cipta %>" selected>
                  <%= hak_cipta %>
                </option>
                <option value="All Rights Reserved">All Rights Reserved</option>
                <option value="Public Domain">Public Domain</option>
              </select>
            </div>

            <!-- Tombol Simpan -->
            <div class="mb-3">
              <button type="submit" class="btn btn-success">Simpan</button>
            </div>
          </form>
        </div>
      </div>
    </div>

    <!-- Bootstrap JS -->
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
